export default function RequestFeaturesPage() {
  return (
    <div className="flex min-h-0 flex-1 flex-col overflow-hidden bg-background">
      <header className="shrink-0 border-b border-border bg-card px-6 py-5">
        <h1 className="font-display text-xl font-extrabold tracking-tight">Request Features</h1>
        <p className="text-muted-foreground mt-1 text-sm">Tell us what to build next.</p>
      </header>
      <div className="text-muted-foreground flex flex-1 items-center justify-center p-8 text-sm">
        Placeholder — connect a form to your backlog or support inbox.
      </div>
    </div>
  );
}
